[package]
name = "crdt-core"
version = "0.1.0"
edition = "2021"

[features]
default = []
serde = ["dep:serde", "uuid/serde"]

[dependencies]
serde = { version = "1", features = ["derive"], optional = true }
uuid = { version = "1", features = ["v4"] }

[dev-dependencies]
proptest = "1"
