import { defineStore } from 'pinia'

export const useTodoStore = defineStore('todo', {
  state: () => ({
    // TODO: items, nodeId, peers, wsConnected
  }),
  actions: {
    // TODO: connect(port), addItem, removeItem, setText, setDone
  },
})
